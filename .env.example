# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Database: set DATABASE_URL to your Postgres connection string.
# Options: Supabase (cloud), local Postgres, or Neon.

# --- Supabase (recommended cloud) ---
# 1. Create project: https://supabase.com/dashboard → New project
# 2. Dashboard → Project Settings → Database → Connection string → "URI" (Session mode, port 5432)
# 3. Replace [YOUR-PASSWORD] with your database password (reset in Database → Database password if needed)
# DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"
# Or direct (IPv6): DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"


# DATABASE_URL="postgresql://postgres:password@localhost:5432/mydb?schema=public"
DATABASE_URL="postgresql://postgres.aemrmljkdupxemvnyfbo:UkVEAwL6LI3usv1G@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres"


PORT=5000

BETTER_AUTH_SECRET="better-auth-secret"
BETTER_AUTH_URL="http://localhost:5000"

APP_URL="http://localhost:3000"
APP_USER="your-email"
APP_PASS="your-app-password"

GOOGLE_CLIENT_SECRET="google-client-secret"
GOOGLE_CLIENT_ID="Your-google-cient-id"

# Optional: admin seed
ADMIN_EMAIL="admin@foodhub.local"
ADMIN_PASSWORD="admin-foodhub"